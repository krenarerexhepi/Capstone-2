<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/topItem"
    android:orientation="vertical"
    android:padding="@dimen/topItem">

    <ImageView
        android:id="@+id/image"
        android:layout_width="@dimen/widthitem"
        android:layout_height="@dimen/widthheight"
        android:scaleType="centerCrop"
        android:contentDescription="@string/descriptionButtonText" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal">

        <TextView
            android:id="@+id/text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center|left"
            android:textSize="@dimen/textSize" />

        <ImageButton
            android:id="@+id/imageButton"
            android:layout_width="@dimen/ImageButtonWidth"
            android:layout_height="@dimen/ImageButtonWidth"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:background="@null"
            android:contentDescription="@string/descriptionButtonText"
            android:src="@android:drawable/star_big_on" />

    </RelativeLayout>

</LinearLayout>
